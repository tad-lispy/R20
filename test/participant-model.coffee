# TODO: convert it to real test suit :P

# Participant = require "../models/Participant"

# joe = new Participant name: "Joe Blow", roles: [ "reader", "sleeper", "Editor", "EDITOR", "reader" ], bio: "He blows. That's what he does.", titles: "MC", emails: [ "joe@blow.com" ]

# joe.validate (error) -> if error then console.log error else console.log "validated"
# # Should pass

# joe = new Participant name: "Joe Blow", roles: [ "reader", "sleeper", "editor" ], bio: "He blows. That's what he does.", titles: "MC", emails: [ "joe@blow.com" ]
# # Should fail